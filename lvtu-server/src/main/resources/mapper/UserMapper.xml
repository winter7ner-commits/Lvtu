<<<<<<< HEAD
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bitzh.lvtu.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.bitzh.lvtu.entity.User">
        <id column="id" property="userId"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="email" property="email"/>
        <result column="user_type" property="userType"/>
        <result column="avatar_url" property="avatarUrl"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, username, password, email, user_type, avatar_url, status, created_at, updated_at
    </sql>

    <select id="findByUsername" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM users
        WHERE username = #{username}
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM users
        WHERE id = #{userId}
    </select>

    <insert id="insert" parameterType="com.bitzh.lvtu.entity.User" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users (username, password, email, user_type, avatar_url, status, created_at, updated_at)
        VALUES (#{username}, #{password}, #{email}, #{userType}, #{avatarUrl}, #{status}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.bitzh.lvtu.entity.User">
        UPDATE users
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="email != null">email = #{email},</if>
            <if test="userType != null">user_type = #{userType},</if>
            <if test="avatarUrl != null">avatar_url = #{avatarUrl},</if>
            <if test="status != null">status = #{status},</if>
            updated_at = NOW()
        </set>
        WHERE id = #{userId}
    </update>

</mapper>
=======
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bitzh.lvtu.mapper.UserMapper">

    <resultMap id="userMap" type="com.bitzh.lvtu.entity.User">
        <id property="userId" column="user_id"/>
        <result property="username" column="username"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="avatarUrl" column="avatar_url"/>
        <result property="userType" column="user_type"/>
        <result property="status" column="status"/>
        <result property="isVerified" column="is_verified"/>
        <result property="authStatus" column="auth_status"/>
        <result property="createdTime" column="created_time"/>
        <result property="updatedTime" column="updated_time"/>
        <result property="region" column="region"/>
    </resultMap>

    <select id="selectByUsername" resultMap="userMap">
        SELECT * FROM users WHERE username = #{username} LIMIT 1
    </select>

    <select id="selectByEmail" resultMap="userMap">
        SELECT * FROM users WHERE email = #{email} LIMIT 1
    </select>

    <select id="selectByPhone" resultMap="userMap">
        SELECT * FROM users WHERE phone = #{phone} LIMIT 1
    </select>

    <select id="selectById" resultMap="userMap">
        SELECT * FROM users WHERE user_id = #{userId} LIMIT 1
    </select>

    <insert id="insertUser" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users
            (username, password_hash, phone, email, avatar_url, user_type, status, is_verified, auth_status, region)
        VALUES
            (#{username}, #{passwordHash}, #{phone}, #{email}, #{avatarUrl}, #{userType}, #{status}, #{isVerified}, #{authStatus}, #{region})
    </insert>

    <update id="updatePassword">
        UPDATE users
        SET password_hash = #{passwordHash}
        WHERE user_id = #{userId}
    </update>

    <update id="updateStatus">
        UPDATE users
        SET status = #{status}
        WHERE user_id = #{userId}
    </update>

</mapper>
>>>>>>> 2c7bb808696ce5aba4b1bc1a4e70731964c3986b
