<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bitzh.lvtu.mapper.PaymentMapper">

    <resultMap id="PaymentResultMap" type="com.bitzh.lvtu.entity.Payment">
        <id property="paymentId" column="payment_id"/>
        <result property="orderId" column="order_id"/>
        <result property="amount" column="amount"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="paymentTime" column="payment_time"/>
    </resultMap>

    <insert id="insert" parameterType="com.bitzh.lvtu.entity.Payment"
            useGeneratedKeys="true" keyProperty="paymentId" keyColumn="payment_id">
        INSERT INTO payment (
            order_id,
            amount,
            status
        ) VALUES (
            #{orderId},
            #{amount},
            #{status}
        )
    </insert>

    <update id="update" parameterType="com.bitzh.lvtu.entity.Payment">
        UPDATE payment
        <set>
            <if test="amount != null">amount = #{amount},</if>
            <if test="status != null">status = #{status},</if>
            <if test="paymentTime != null">payment_time = #{paymentTime},</if>
        </set>
        WHERE payment_id = #{paymentId}
    </update>

    <delete id="deleteById">
        DELETE FROM payment
        WHERE payment_id = #{paymentId}
    </delete>

    <select id="selectById" resultMap="PaymentResultMap">
        SELECT
            payment_id,
            order_id,
            amount,
            status,
            create_time,
            payment_time
        FROM payment
        WHERE payment_id = #{paymentId}
    </select>

    <select id="selectByStatus" resultMap="PaymentResultMap">
        SELECT
            payment_id,
            order_id,
            amount,
            status,
            create_time,
            payment_time
        FROM payment
        WHERE status = #{status}
        ORDER BY create_time DESC
    </select>

    <select id="selectByOrderId" resultMap="PaymentResultMap">
        SELECT
            payment_id,
            order_id,
            amount,
            status,
            create_time,
            payment_time
        FROM payment
        WHERE order_id = #{orderId}
        ORDER BY create_time DESC
    </select>
</mapper>
