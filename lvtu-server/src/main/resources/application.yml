spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lvtu
  
#    修改为自己的数据库名字和密码
#    冒号和信息中间要加个空格
    username: yc
    password: Yc20050609!
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  servlet:
    multipart:
      max-file-size: 10MB     # 单个文件最大大小
      max-request-size: 12MB  # 总请求最大大小

mybatis:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: com.bitzh.lvtu.entity
  type-handlers-package: com.bitzh.lvtu.config
  configuration:
    map-underscore-to-camel-case: true

pagehelper:
  helperDialect: mysql
  reasonable: true
  supportMethodsArguments: true
  params: count=countSql

server:
  port: 8080

file:
  upload-dir: lvtu_uploads
  access-path: /uploads/**       # 访问路径映射

web:
  upload-path: lvtu_uploads/avatars/

